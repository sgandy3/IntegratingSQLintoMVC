@model IEnumerable<PiesPiesPies.Models.Sale>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pie.Flavor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pie.Flavor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalesID })
        </td>
    </tr>
}

</table>
